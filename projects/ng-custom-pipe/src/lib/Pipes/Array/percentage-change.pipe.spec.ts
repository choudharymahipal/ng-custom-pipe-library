import { PercentageChangePipe } from './percentage-change.pipe';

describe('PercentageChangePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageChangePipe();
    expect(pipe).toBeTruthy();
  });
});
